[project]
name = "fastapi-pet-project"
version = "0.1.0"
description = "a chill pet project where i mess around with fastapi, try stuff out, and break things on purpose"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.124.0",
    "pydantic>=2.12.5",
    "sqlalchemy>=2.0.44",
    "uvicorn>=0.38.0",
]
